Python - Almost a circle
A Base class that manages ids and JSON serialization, a Rectangle class with validated private attributes, and a Square class built on top of it. The whole package is covered by unit tests in tests/.

Run the test suite with:

python3 -m unittest discover tests
